<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Navbar</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse"
          data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
          aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= active_link_to 'Home', root_path, class: 'nav-link',
                           active: :exclusive %>
      </li>
      <li class="nav-item">
        <%= active_link_to 'Cursos', courses_path, class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <%= active_link_to 'Professores', teachers_path, class: 'nav-link' %>
      </li>
    </ul>
    <% if user_signed_in? %>
      <%= render 'layouts/user_links' %>
    <% else %>
      <%= active_link_to 'Cadastrar', new_user_registration_path,
                         class: 'btn btn-outline-secondary my-2 my-sm-0' %>
      <%= active_link_to 'Entrar', new_user_session_path,
                         class: 'btn btn-outline-primary my-2 ml-2 my-sm-0' %>
    <% end %>

  </div>
</nav>