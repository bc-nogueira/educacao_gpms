<%= form_with model: Wish.new do |f| %>
  <%= f.hidden_field :course_id, value: course.id %>
  <%= f.hidden_field :student_id, value: current_user.student.id %>
  <%= f.submit 'Adicionar a lista de desejo', class: 'btn btn-primary w-100' %>
<% end %>