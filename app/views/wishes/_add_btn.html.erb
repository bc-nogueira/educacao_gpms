<%= form_with model: Wish.new, class: 'mt-3' do |f| %>
  <%= f.hidden_field :course_id, value: course.id %>
  <%= f.hidden_field :student_id, value: current_user.student.id %>
  <%= f.submit 'Adicionar a lista de desejo', class: 'btn btn-primary' %>
<% end %>